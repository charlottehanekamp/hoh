class Agenda < ActiveRecord::Base
     def monthly_update
         # @user = User.all
         # @u/ser.each do |u|
           # AgendaMailer.monthly_mail(u).deliver
         # end
         # AgendaMailer.monthly_mail().deliver_now

     end

end
